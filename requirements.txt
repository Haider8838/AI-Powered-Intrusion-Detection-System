# AI-Powered IDS — Python dependencies
# Install with:  pip install -r requirements.txt

# Core ML / Data
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Network / Packet capture
scapy>=2.5.0

# LLM APIs
anthropic>=0.40.0
google-genai>=1.0.0

# Notebook support (optional — needed only if running the .ipynb file)
# NOTE: On Windows, enable Long Paths first:
#   Run as Admin: reg add "HKLM\SYSTEM\CurrentControlSet\Control\FileSystem" /v LongPathsEnabled /t REG_DWORD /d 1 /f
# Then run: pip install ipykernel jupyter
# ipykernel>=6.29.0
# jupyter>=1.0.0

# Web server / dashboard
flask>=3.0.0
flask-socketio>=5.3.0

# Environment / utilities
python-dotenv>=1.0.0
